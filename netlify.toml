[build]
  command = "mkdir -p dist && cp index.html dist/ && cp client-portal.html dist/ && cp -r images dist/images && echo 'Build complete'"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
